# Backend monitoring sidecar service dependencies
#
# Python version compatibility:
#   Python 3.8+  : use flask>=2.3,<3.1 and psutil>=5.9
#   Python 3.6/3.7: use the versions below (flask 2.0.x is the last series supporting Python 3.6)
#
# These versions are tested with Python 3.6.8+

dataclasses; python_version < "3.7"  # stdlib in 3.7+, backport needed for 3.6
flask==2.0.3
werkzeug==2.0.3  # Flask 2.0.x requires Werkzeug <2.1; newer Werkzeug breaks the import
psutil==5.9.5
requests==2.28.2
python-ldap==3.4.4  # Optional — remove if LDAP group check endpoint not needed
